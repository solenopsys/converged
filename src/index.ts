import { createSignal, lazy, createEffect, createRoot, createMemo, createComputed, Show, Component } from "solid-js";

export {
  createSignal as signal,
  createRoot as root,
  createMemo as memo,
  createComputed as computed,
  lazy,
  createEffect as effect,
  Show,
  Component,
}