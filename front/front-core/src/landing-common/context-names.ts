/**
 * Context keys resolved by contexts-ms as `<lang>/<key>.json`.
 * Single source of truth — never copy these strings around the code.
 */
export const CHAT_CONTEXT = "chat";
export const VOICE_CONTEXT = "voice";
