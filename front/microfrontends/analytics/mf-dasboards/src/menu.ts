import { SHOW_DASHBOARD } from './functions';

export const GROUP = { id: "analytics", title: "Analytics", iconName: "IconChartBar" };

export const MENU = {
    "title": "menu.dashboard",
    "iconName": "IconLayoutDashboard",
    "action": SHOW_DASHBOARD
};
