export const ID = 'calls-mf';
export { MENU } from "./menu";
import {BasePlugin} from "front-core";

// TODO: Add actions when calls module is implemented
const ACTIONS: any[] = [];

export default new BasePlugin(ID, ACTIONS);
