class w{name;scope;inputs=new Map;expectedInputs=new Set;constructor(j,b=[]){this.name=j;b.forEach((f)=>this.expectedInputs.add(f))}async execute(j,b){if(b)this.inputs.set(b,j);if(this.expectedInputs.size>0){let h=Array.from(this.inputs.keys());if(!Array.from(this.expectedInputs).every((q)=>h.includes(q)))return null}let f={};for(let[h,k]of this.inputs.entries())f[h]=k;return this.inputs.clear(),f}}export{w as MergeNode};
