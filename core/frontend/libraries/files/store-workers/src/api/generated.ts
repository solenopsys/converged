export {
	createStoreServiceClient,
	metadata,
	storeClient,
} from "g-store";

export type {
	BlockMetadata,
	CacheRef,
	CompressionType,
	HashString,
	PaginatedResult,
	PaginationParams,
	StoreServiceClient,
} from "g-store";
