// Generated by nrpc/src/zig-generator.ts. Do not edit manually.
const std = @import("std");
const transport = @import("transport");

pub const service = "resonus";

pub fn bind(runtime: *transport.Runtime, handler: transport.RuntimeHandler) !void {
    return runtime.bind(service, handler);
}

/// Server-side NRPC descriptor. A native application calls verifyAndAuthorize
/// before its handwritten handler; this generated code never knows the handler.
pub const method_policies = [_]transport.auth.authorize.MethodPolicy{
    .{ .service = service, .method = "savePhoneNumber", .level = .user, .mode = null },
    .{ .service = service, .method = "updatePhoneNumber", .level = .user, .mode = null },
    .{ .service = service, .method = "getPhoneNumber", .level = .user, .mode = null },
    .{ .service = service, .method = "deletePhoneNumber", .level = .user, .mode = null },
    .{ .service = service, .method = "listPhoneNumbers", .level = .user, .mode = null },
    .{ .service = service, .method = "getPrimaryPhoneNumber", .level = .user, .mode = null },
    .{ .service = service, .method = "saveLlmGateConfig", .level = .user, .mode = null },
    .{ .service = service, .method = "getLlmGateConfig", .level = .user, .mode = null },
    .{ .service = service, .method = "listLlmGateConfigs", .level = .user, .mode = null },
    .{ .service = service, .method = "deleteLlmGateConfig", .level = .user, .mode = null },
};

pub fn policy(method: []const u8) ?transport.auth.authorize.MethodPolicy {
    for (method_policies) |entry| {
        if (std.mem.eql(u8, entry.method, method)) return entry;
    }
    return null;
}

pub fn verifyAndAuthorize(
    allocator: std.mem.Allocator,
    raw_token: []const u8,
    verifier: transport.auth.jwt.Config,
    now_unix: i64,
    method: []const u8,
) !transport.auth.jwt.VerifiedToken {
    const method_policy = policy(method) orelse return error.MethodUnavailable;
    var token = try transport.auth.jwt.verify(allocator, raw_token, verifier, now_unix);
    errdefer token.deinit(allocator);
    try transport.auth.authorize.authorize(token.toClaims(), method_policy);
    return token;
}

pub fn savePhoneNumber(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "savePhoneNumber",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn updatePhoneNumber(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "updatePhoneNumber",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn getPhoneNumber(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "getPhoneNumber",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn deletePhoneNumber(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "deletePhoneNumber",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn listPhoneNumbers(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "listPhoneNumbers",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn getPrimaryPhoneNumber(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "getPrimaryPhoneNumber",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn saveLlmGateConfig(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "saveLlmGateConfig",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn getLlmGateConfig(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "getLlmGateConfig",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn listLlmGateConfigs(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "listLlmGateConfigs",
        .scope = scope,
        .body = body,
    }, completion);
}

pub fn deleteLlmGateConfig(
    runtime: *transport.Runtime,
    scope: []const u8,
    body: []const u8,
    completion: transport.RuntimeCompletion,
) !void {
    return runtime.request(.{
        .service = service,
        .method = "deleteLlmGateConfig",
        .scope = scope,
        .body = body,
    }, completion);
}
