# PARAMS

Native GLiNER2 parameter extraction service.

The `models/params` directory contains the ONNX model and tokenizer used by
the native runtime. Python is intentionally limited to the acceptance tests
in `test/`.
