# Microfrontends Group: automation

## Purpose
Groups automation microfrontends for DAG workflows, schedules, and webhooks. The modules let users configure automation, inspect its state, and act on delivery or execution history without embedding job-runner logic in the frontend.

## Responsibility Boundary
Owns automation UI grouping and UI-level workflows; does not own backend job execution engine.
