import type { FieldConfig } from "front-core";

export const structColumns: FieldConfig[] = [
  {
    id: "path",
    title: "Path",
    type: "text",
    tableVisible: true,
    formVisible: true,
  },
];
