# Surface: automation

## Purpose
Provides one automation workspace for DAG workflows, schedules, and webhooks. It lets users configure automation, inspect its state, and act on delivery or execution history without embedding job-runner logic in the frontend.

## Responsibility Boundary
Owns automation UI grouping and UI-level workflows; does not own backend job execution engine.
