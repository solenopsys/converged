# Repositories Group: data

## Purpose
Groups storage-facing repositories for files, exports, and generic service data. Stateless transformations belong to `modules/lambdas/data`.

## Responsibility Boundary
Owns data service boundaries at service layer; does not own business validation rules for each domain.
