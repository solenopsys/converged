# Converged Repositories

## Purpose
Contains stateful backend modules grouped by business and technical domains. Each repository owns domain data access, persistence, and the service contract around that data.

## Responsibility Boundary
Owns persistent domain state and its backend API; does not own frontend composition or stateless integrations.
