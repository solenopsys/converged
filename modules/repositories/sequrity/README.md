# Repositories Group: sequrity

## Purpose
Groups stateful backend modules for authentication, identity, authorization, and environment-scoped configuration. Stateless secret-provider access belongs to `modules/lambdas/sequrity`.

## Responsibility Boundary
Owns security-domain service boundaries; does not own non-security domain business entities.
